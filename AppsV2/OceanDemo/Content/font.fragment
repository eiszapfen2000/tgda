#version 150
#extension GL_ARB_explicit_attrib_location : enable

in vec2 texcoordsOut;
layout(location = 0) out vec4 color;

void main()
{
	vec4 sample = texture2D(characterpage, texcoordsOut);
	color = vec4(sample.rgb * textcolor.rgb, sample.a * textcolor.a);
}

